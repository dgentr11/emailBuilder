# Sanity (required for CMS)
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: static password protection for non-Studio pages
# When set, /, /generate, /guide require login. /studio and /login stay public.
# APP_PASSWORD=your-shared-team-password
